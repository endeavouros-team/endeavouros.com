# Supply-chain posture for a project that was compromised through a plugin
# ecosystem. See ../docs/security.md.
ignore-scripts=true    # no package executes arbitrary code on install
save-exact=true        # package.json pins exact versions, not ranges
package-lock=true
engine-strict=true
fund=false
