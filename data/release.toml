# The current EndeavourOS release.
#
# PUBLISHING A NEW RELEASE: edit the [current] values below, and the `magnet`
# and `torrent` lines under them -- they carry the ISO filename too. Then run
# `make check` and `make mirrors`. Every mirror URL, every shell command in the
# verification instructions, and the download buttons regenerate from these.
# No HTML is ever touched.

[current]
codename  = "Titan Nova"
date      = 2026-08-15
iso       = "EndeavourOS_Titan-Nova-2026.08.15.iso"
sha512    = "3f83bd745e9d5d518bdf6fc9a24092fb5347da449a6e109c6572bf999097b8b548c4cf1848fdfb3c7c2823f558c3f5b51a127060d9c5b81b9ad89b4041bee7fa"
sizeBytes = 3898638336

# Suffixes appended to `iso` to build the checksum and signature URLs.
#
# Upstream renamed the checksum file at Titan Nova: it is `.sha512sum`, not
# `.sha512`, on every mirror. Because the URLs are composed rather than stored,
# that rename is this one line -- `make mirrors` is what proves it across all 26.
sha512Suffix = ".sha512sum"
sigSuffix    = ".sig"

magnet  = "magnet:?xt=urn:btih:0866fdf2d998c4fafe043e6321c678c07ab674d6&dn=EndeavourOS_Titan-Nova-2026.08.15.iso&tr=udp%3A%2F%2Ftracker.opentrackr.org%3A1337%2Fannounce&tr=udp%3A%2F%2Fopen.stealth.si%3A80%2Fannounce&tr=udp%3A%2F%2Ftracker.torrent.eu.org%3A451%2Fannounce&tr=udp%3A%2F%2Fexodus.desync.com%3A6969%2Fannounce&tr=udp%3A%2F%2Fopen.demonii.com%3A1337%2Fannounce&tr=udp%3A%2F%2Ftracker.openbittorrent.com%3A6969%2Fannounce"
torrent = "https://mirror.rznet.fr/endeavouros/iso/EndeavourOS_Titan-Nova-2026.08.15.iso.torrent"

# The key the ISO is signed with. Users verify against this fingerprint, so it
# is rendered verbatim and must never be edited casually.
[current.signing]
name        = "Johannes Kamprad"
email       = "joekamprad@endeavouros.com"
fingerprint = "8F43 FC37 4CD4 CEEA 19CE E323 E3D8 752A CDF5 95A1"
shortKey    = "CDF595A1"
keyserver   = "keyserver.ubuntu.com"

[current.requirements]
diskGb = 15
ramGb  = 4
cpu    = "64-bit dual-core Intel or AMD processor"
note   = "The installer still handles older machines, but modern EFI (UEFI) systems without legacy CSM mode work best."
