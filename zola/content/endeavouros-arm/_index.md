+++
title = "EndeavourOS ARM"
template = "prose.html"
sort_by = "none"
+++

The ARM section was built on the Astro track only, after the toolchain decision was made. It reads `data/arm-images.toml`, which this track has no template for.

Like the news section, this page exists so the shared navigation in `data/site.toml`
does not lead to a dead end on this build. Read it on the Astro build.
