+++
title = "News"
template = "prose.html"
sort_by = "none"
+++

The news section was built on the Astro track only. It is the first content the two
tracks do not share, because the toolchain decision had already been made when the
project lead set the migration scope — see `docs/bake-off.md` and `docs/status.md`.

This page exists so the shared navigation in `data/site.toml` does not lead to a dead
end here. Read the announcements on the Astro build.
